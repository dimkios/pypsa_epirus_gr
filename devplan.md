# devplan.md — Πλάνο Ανάπτυξης: PyPSA Epirus GR

> Αυτό το αρχείο προέκυψε από συζήτηση πάνω στο [plan.md](plan.md). Καταγράφει ΠΩΣ θα δουλέψουμε, όχι κώδικα. Ξεκινάμε υλοποίηση μόνο όταν το πεις ρητά ("ξεκινάμε Phase 0" κ.ο.κ.). Κάθε βήμα εκτελείται μόνο μετά από δική σου έγκριση.

## 0. Σκοπός του εγγράφου
Έγγραφο αναφοράς για την πορεία του έργου. Ενημερώνεται σταδιακά — δεν είναι "κλειδωμένο". Όταν αλλάζουμε κατεύθυνση, το ενημερώνουμε μαζί.

## 1. Όραμα
Εφαρμογή βελτιστοποίησης παραγωγής/κατανάλωσης ηλεκτρικής ενέργειας για την Ήπειρο με χρήση PyPSA, με στόχο μάθηση και σταδιακή εξέλιξη σε πλήρη web εφαρμογή. Φιλόδοξο μελλοντικό βήμα: επέκταση σε "PyPSA GREECE" (επίπεδο χώρας).

## 2. Κανόνες συνεργασίας
- Μάθηση πρώτα: κάθε βήμα συνοδεύεται από εξήγηση του "γιατί", όχι μόνο του "πώς". Ξέρεις ήδη Python — οι εξηγήσεις θα εστιάζουν σε PyPSA, Streamlit, Git/GitHub.
- **Τίποτα δεν εκτελείται (κώδικας, εγκαταστάσεις, git actions) χωρίς ρητή έγκρισή σου.** Θα ρωτάω πριν από κάθε ουσιαστικό βήμα.
- Εσωτερικό ιστορικό: κρατάω context στη συνομιλία μας· η μόνιμη τεκμηρίωση πάει στο Obsidian vault (§6).
- Commits συχνά, με μικρά βήματα, ώστε το ιστορικό στο GitHub να είναι διδακτικό/αναγνώσιμο.

## 3. Τεχνολογικό stack (αρχικό)
| Στοιχείο | Επιλογή | Σημείωση |
|---|---|---|
| Γλώσσα | Python 3.11+ | θα επιβεβαιώσουμε ποια έκδοση έχεις εγκατεστημένη |
| Env management | venv (απλό, ενσωματωμένο) | conda εναλλακτικά αν προτιμήσεις |
| Μοντελοποίηση δικτύου | PyPSA | βασική βιβλιοθήκη optimal power flow |
| UI (Φάση 1-3) | Streamlit | ταχεία ανάπτυξη, learning-friendly |
| Χάρτης | plotly / folium (θα διαλέξουμε στην πράξη στο Phase 1) | |
| Solver | HiGHS (open-source, έρχεται έτοιμος με PyPSA) | αποφεύγουμε εμπορικά solvers αρχικά |
| Έλεγχος εκδόσεων | Git + GitHub (dimkios/pypsa_epirus_gr) | |
| Τεκμηρίωση | Obsidian vault (τοπικό, ξεχωριστό από τον κώδικα) | |

## 4. Στρατηγική δεδομένων
Αποφασίστηκε: **custom απλοποιημένο δίκτυο**, όχι απευθείας extraction από pypsa-eur (πολύ βαρύ/πολύπλοκο για αρχή). Το pypsa-eur θα χρησιμεύσει αργότερα ως πηγή μεθοδολογίας/έμπνευσης (Phase 5, προαιρετικό).

- **Κόμβοι (buses)**: αρχικά 3-5, ένας ανά νομό/μεγάλη πόλη της Ηπείρου (π.χ. Ιωάννινα, Άρτα, Πρέβεζα, Θεσπρωτία — θα επιβεβαιωθεί η τελική λίστα).
- **Δεδομένα παραγωγής/κατανάλωσης/γραμμών**: ξεκινάμε με **εκτιμήσεις/δημόσια στοιχεία** (π.χ. δημοσιευμένα στατιστικά ΑΔΜΗΕ/ΔΕΔΔΗΕ/ΕΛΣΤΑΤ, reports ΡΑΑΕΥ), τα ρυθμίζουμε/βελτιώνουμε σταδιακά όσο βρίσκουμε ακριβέστερα στοιχεία. Κάθε πηγή δεδομένων που χρησιμοποιείται τεκμηριώνεται στο Obsidian με ημερομηνία/link.
- Δεν θεωρούμε τα αρχικά νούμερα "ακριβή" — είναι εκπαιδευτικό μοντέλο πρώτα, ρεαλιστικό αργότερα.

## 5. Φάσεις ανάπτυξης (roadmap)

**Phase 0 — Setup**
- Δημιουργία local git repo + σύνδεση με GitHub (dimkios/pypsa_epirus_gr) ✅ (σήμερα, μαζί με αυτό το αρχείο)
- venv + βασικά dependencies (streamlit, pypsa, pandas, plotting lib)
- Δομή φακέλων repo (π.χ. `app/`, `data/`, `notebooks/`, `docs/`)
- Δομή Obsidian vault (§6)

**Phase 1 — Streamlit + στατικός χάρτης Ηπείρου**
- Πολύ απλό Streamlit app, χάρτης με 3-5 hardcoded κόμβους, χωρίς PyPSA ακόμα.
- Μαθησιακός στόχος: Streamlit βασικά (layout, widgets), mapping library.

**Phase 2 — Πρώτο PyPSA μοντέλο**
- Πάνω στο ίδιο δίκτυο: buses, lines, generators (ΑΠΕ + συμβατικά/εισαγωγές), loads.
- Static/deterministic optimization (ένα snapshot ή απλό time series).
- Οπτικοποίηση αποτελεσμάτων (παραγωγή ανά πηγή, ισοζύγιο) στο Streamlit.
- Μαθησιακός στόχος: βασικές έννοιες PyPSA (Network, components, `.optimize()`).

**Phase 3 — Παραμετροποίηση από χρήστη**
- Sliders/inputs: % αύξηση ΑΠΕ (φωτοβολταϊκά/αιολικά), ανεξαρτητοποίηση από εισαγωγές, χωρητικότητα αποθήκευσης, αύξηση ζήτησης.
- Rerun optimization on-demand, σύγκριση σεναρίων.
- Μαθησιακός στόχος: PyPSA constraints/scenario design, Streamlit interactivity/state.

**Phase 4 — Ρεαλισμός δεδομένων**
- Αντικατάσταση εκτιμήσεων με πιο ακριβή δημόσια στοιχεία (ΑΔΜΗΕ/ΔΕΔΔΗΕ time series αν διαθέσιμα, καιρικά δεδομένα για ΑΠΕ κ.λπ.).

**Phase 5 — pypsa-eur exploration (προαιρετικό)**
- Εξέταση αν/πώς μπορούμε να αντλήσουμε/επικυρώσουμε στοιχεία από pypsa-eur workflow, χωρίς να αντικαταστήσουμε το custom μοντέλο.

**Phase 6 — Μετάβαση σε πλήρες web app**
- Συζήτηση αρχιτεκτονικής (frontend framework, backend API, hosting) όταν φτάσουμε εκεί.

**Phase 7 — Φιλόδοξο: PyPSA GREECE**
- Επέκταση μεθοδολογίας σε επίπεδο χώρας, μετά την ωρίμανση του μοντέλου Ηπείρου.

## 6. Τεκμηρίωση — Obsidian vault
Vault: `D:\jim\UOI-ΠΜΣ\UOI_KIOSSES\ENERGY AND PYPSA\PYPSA_EPIRUS\pypsa_epirus_gr`

Προτεινόμενη δομή (θα την προσαρμόσουμε στην πράξη):
- `00-Index.md` — πίνακας περιεχομένων/κατάσταση έργου
- `01-Decisions/` — ένα note ανά σημαντική απόφαση (τι αποφασίστηκε, γιατί, εναλλακτικές)
- `02-Data-Sources/` — πηγές δεδομένων, ημερομηνία άντλησης, links
- `03-Concepts/` — σημειώσεις μάθησης (π.χ. "τι είναι bus/line/generator στο PyPSA")
- `04-Sessions/` — σύντομο log ανά session δουλειάς (ημερομηνία, τι έγινε, επόμενα βήματα)

## 7. Git/GitHub workflow
- Repo: https://github.com/dimkios/pypsa_epirus_gr.git
- Branch `main` για σταθερή δουλειά· μπορούμε να συζητήσουμε feature branches όταν αρχίσουν τα Phases.
- Commits μικρά και συχνά, μηνύματα στα Ελληνικά ή Αγγλικά (ό,τι προτιμάς) περιγράφοντας το "τι" και σύντομα το "γιατί".
- `.gitignore` για venv, `__pycache__`, τοπικά data caches κ.λπ.

## 8. Ανοιχτά σημεία προς συζήτηση αργότερα
- Ακριβής λίστα κόμβων/νομών Ηπείρου.
- Ποια mapping library (plotly vs folium) — θα δοκιμάσουμε στο Phase 1.
- Πηγές πραγματικών δεδομένων ΑΔΜΗΕ/ΔΕΔΔΗΕ όταν φτάσουμε στο Phase 4.
- Αρχιτεκτονική πλήρους web εφαρμογής (Phase 6).
